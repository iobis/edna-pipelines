// Applied to the nested nf-core/ampliseq run (see RUN_AMPLISEQ in main.nf).
// Keeps ampliseq self-contained; only sets Docker platform for Apple Silicon hosts.

docker {
    enabled = true
    platform = 'linux/amd64'
}
